# Core ML - Install PyTorch with CUDA 13.0 (cu130) first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu130
torch>=2.1.0
torchvision>=0.16.0
# Use 4.x for InternVL, Qwen2.5-VL, LLaVA-Med, Med-Gemma compatibility (avoids meta tensor / GenerationMixin issues)
# Qwen3.5-VL-27B requires transformers>=5.2.0; use a separate venv if needed
transformers>=5.2.0
accelerate>=0.30.0
bitsandbytes>=0.43.0
# unsloth: incompatible with transformers>=5.2.0 and PyTorch 2.10 (flex_attention duplicate template).
# Omit or pin transformers<=4.57.6 if you need Unsloth for qwen3.5-vl-27b.
datasets>=2.18.0

# HEDGE algorithms and utilities (vLLM optional - we use transformers fallback on Windows)
scikit-learn>=1.3.0
albumentations>=1.4.0
sentence-transformers>=2.2.0
optuna>=3.5.0
opencv-python>=4.8.0
joblib>=1.3.0
pandas>=2.0.0
tqdm>=4.66.0
scipy>=1.11.0

# hedge-bench for algorithms (clustering, metrics) - install without vLLM for Windows
# pip install hedge-bench  # may fail on Windows due to vLLM; we implement inference ourselves
